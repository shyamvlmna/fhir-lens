# =============================================================================
# FHIR Renderer - Data Source Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# Data Source Type
# -----------------------------------------------------------------------------
# Choose where to load FHIR bundles from:
#   - 'local': Load from local resources directory (default)
#   - 'api': Load from a remote API endpoint
# -----------------------------------------------------------------------------
VITE_DATA_SOURCE_TYPE=local

# -----------------------------------------------------------------------------
# API Data Source Configuration (when using type='api')
# -----------------------------------------------------------------------------
# Base URL for your FHIR API
# Example: https://api.example.com/fhir/v1
VITE_DATA_SOURCE_API_URL=

# API request timeout in milliseconds (default: 30000 = 30 seconds)
VITE_DATA_SOURCE_API_TIMEOUT=30000

# -----------------------------------------------------------------------------
# Local File Data Source Configuration (when using type='local')
# -----------------------------------------------------------------------------
# Path to local resources directory (default: /resources)
VITE_LOCAL_RESOURCE_PATH=/resources

# =============================================================================
# NHCX API Configuration (Optional - for API integrations)
# =============================================================================
# Base URL for NHCX API services
VITE_NHCX_API_BASE_URL=

# Polling interval for async operations (milliseconds)
VITE_NHCX_POLLING_INTERVAL=30000

# Maximum polling attempts before timeout
VITE_NHCX_MAX_POLLING_ATTEMPTS=120

